version: "3.9"

services:
  api:
    build: .
    container_name: mini_rag_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./docs:/app/docs:ro
    restart: unless-stopped
